# EdgeEnv v0.1.4 Bilingual Docs Sanity Sweep

> Language: [English overview](language.md#english-overview) | [한국어 진입](ko/README.md)

## 1. WHAT - What This Document Defines

This is a short sanity sweep of the bilingual documentation path after the README guide map was split into the English representative path, operational records, and design references.

It checks the first-reader flow only. It does not introduce new CLI behavior, schema behavior, runner behavior, or release scope.

## 2. CONTENTS - Checked Surfaces

- `README.md`
- `docs/ko/README.md`
- `docs/language.md`
- `docs/release-follow-up-v0.1.4.md`
- `docs/portfolio-demo-path.md`
- `docs/local-command-contract.md`
- `docs/compare-workflow-guide.md`
- `docs/export-import-design.md`
- `docs/schema-versioning-migration-policy.md`
- `docs/release-maintenance-checklist.md`

Review method: manual reading-path check, Markdown link existence check, CLI smoke, and pytest.

## 3. HOW - Reading Path Check

Status: passed.

Root README:

- The language selector is visible before the project definition.
- The one-line definition uses the current positioning: local-first run evidence registry and comparability checker.
- `Start Here For v0.1.4` gives a short first path before the full Quickstart.
- Non-goals are visible before users reach deeper examples.
- The Guide Map separates the English representative path from operational records and design references.

Korean README:

- The language selector links back to the English README.
- The Korean page mirrors the same Guide Map grouping.
- The page clearly states that it is a concise Korean entry point and that detailed evidence/design documents use the English representative path.
- The non-goals remain explicit in Korean and avoid OS, VM, Docker, WSL, SSH, cloud, leaderboard, upload-server, or composite-ranking scope drift.

Representative docs:

- The release follow-up note explains what users can trust at the current baseline.
- The portfolio demo path gives a reviewer-friendly fake/local/compare/export-import/bundle-summary route.
- The local command contract explains how external benchmark commands connect through stdout contracts.
- The compare workflow keeps comparability judgement before metric deltas.
- The export/import design keeps evidence portability artifact-first.
- The schema policy keeps compatibility conservative and rejects future schema versions explicitly.
- The release maintenance checklist defines repeatable quality gates.

Validation commands:

```bash
python -m inferedge_env.cli doctor
python -m pytest -q
```

## 4. HOW NOT - Boundaries

- Do not treat this sweep as a new feature gate.
- Do not add unsupported target claims while improving document flow.
- Do not translate every operational record just to prove bilingual support; the Korean README is the concise entry path, while representative evidence documents remain English-first.
- Do not weaken the core positioning: InferEdgeEnv records benchmark evidence and judges comparability. InferEdgeLab remains the validation/decision layer.

## 5. WHERE - Next Review Points

Revisit this sweep when one of these changes:

- README Quickstart command order changes.
- Korean README Guide Map diverges from the root README.
- A new representative English document replaces one of the current start-path documents.
- A release changes the trusted baseline from `v0.1.4`.

## 6. WHY - Background Judgment

The documentation structure is now stable enough that the highest-value check is not another feature or another large guide. The useful question is whether a first reader can move from the root README to the Korean entry point and then to the representative English documents without losing the project boundary.

This sweep confirms that the current path is coherent: English is the representative documentation path, Korean is a concise local-language entry point, and historical operational records remain available without crowding the first screen.

## 7. LEARNED CAUTIONS - Learned Cautions

_(None yet)_
